name: Environments - AWS Bootstrap - Pull Request

on:
  pull_request:
    paths:
      - 'environments/bootstrap/aws/**'
      - '.github/workflows/environments-aws-bootstrap-pr.yml'
      
defaults:
  run:
    working-directory: ./environments/bootstrap/aws

jobs:
  bump_version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run Tests
        run: |
          echo "No tests"
          