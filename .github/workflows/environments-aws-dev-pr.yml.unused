name: Environments - AWS Dev - Pull Request

on:
  pull_request:
    paths:
      - 'environments/aws/dev/**'
      - '.github/workflows/environments-aws-dev-pr.yml'
      
defaults:
  run:
    working-directory: ./environments/aws/dev

jobs:
  bump_version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run Tests
        run: |
          echo "No tests"
          